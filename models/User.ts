// ข้อมูล user ที่จะสร้าง
// id, firstname, lastname, email, password, avatar, created_at, updated_at
interface User {
    id: number
    firstname: string
    lastname: string
    email: string
    password: string
    avatar: string
    created_at: string
    updated_at: string
}

export default User